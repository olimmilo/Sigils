from PIL import Image, ImageDraw

ROT = 0
SHIFT = 40
CANVAS = 1000
BUFFER = 100

def DrawSymbol(R, S, C, B):
  return()

symbol = Image.new("RGB", (CANVAS,CANVAS), "white")
drawsymbol = ImageDraw.Draw(symbol)
